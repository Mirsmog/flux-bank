package com.fluxbank.common.events;

/**
 * Marker interface for all domain events published across flux-bank services.
 * Every class implementing this interface should also extend {@link BaseEvent}.
 */
public interface DomainEvent {
}
